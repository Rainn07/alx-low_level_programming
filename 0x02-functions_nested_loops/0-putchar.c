#include <stdio.h>

/**
 * main - prints a single character c to the stdout
 *
 * @ param c: the character to be printed
 *
 * Return: 0 (Success)
 */
void main(void)
{
	char str[] = "_putchar";
	int len = stren(str);

	for int i = 0; i < len; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
